.env
__pycache__/
*.pyc
.venv/
venv/

# Claude Code 로컬 설정(개발서버 실행 정의) — 저장소에 넣지 않음
.claude/

# profile.py 로컬 실행 결과(매물대 PNG) — 대시보드가 본체라 저장소엔 불필요
profile_*.png
cgo_dry.csv

# factor.py 로컬 캐시(94만 행) — 매번 5분씩 받지 않으려는 것뿐이라 저장소엔 불필요
factor_cache.pkl
marketmode_cache.pkl
flow_cache.pkl
flowcap_cache.pkl

# 유튜브 자막 등 외부 텍스트 — 검토용으로만 두고 저장소엔 올리지 않는다(저작권)
*.txt

# dart.py 로컬 캐시(corpCode 3.5MB) — 매 실행마다 다시 받으면 DART가 IP를 막는다
.cache/
guru_dry.csv
